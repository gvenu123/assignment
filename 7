#include <stdio.h>

int main() {
    int num, count = 0;

    printf("Input num: ");
    scanf("%d", &num);

    while(num != 0) {
        count++;
        num /= 10;
    }

    printf("Number of digits: %d\n", count);

    return 0;
}
output:Input num: 35419
Number of digits: 5
